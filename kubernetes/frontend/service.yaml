apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: prex-demo
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
